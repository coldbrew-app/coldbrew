node_modules
.pnpm-store
dist
.output
.vercel
.env
.tanstack
.fallow
.DS_Store
*.tsbuildinfo
*.local
*.local.*
.env.keys
.deployed-sha

# Terraform working directories, local state, and saved plans. Provider lock
# files remain tracked so CI and local runs select identical artifacts.
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
crash.log
crash.*.log

# Only durable Impeccable surface briefs belong in the repository.
.impeccable/*
!.impeccable/surface-briefs/
.impeccable/surface-briefs/*
!.impeccable/surface-briefs/*.md
